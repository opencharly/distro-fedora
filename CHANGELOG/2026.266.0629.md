# 2026.266.0629 — feat: remove the preempt arbiter beds relocated to plugin-preempt

## Summary of changes

Remove the three resource-arbiter R10 witness beds from `distro-fedora/charly.yml` now that they live in the plugin's owning repo (`opencharly/plugin-preempt`):

- `check-preempt-arbiter-pod`
- `check-preempt-live-pod`
- `check-preempt-vm-live`

**WHY.** A check bed must live in the repo that owns what it tests. These beds exercise `candy/plugin-preempt`'s `verb:arbiter`, so they moved to the plugin repo (opencharly/plugin-preempt#8, merged) with their own minimal fixture (`check-preempt-fixture` + `check-preempt-keepalive` + a local `web-vm`). This is the **source-side half** of that relocation — a surviving duplicate would be the R3/R5 violation the move eliminates.

**Scope discipline: `web-vm` is KEPT.** `distro-fedora`'s `web-vm` is ALSO the fixture for `check-cross-vm-http` (and the other cross beds), so only the three preempt bed entities and the comment banners that documented THEM are removed. `web-vm` and `check-cross-vm-http` (including its own doc banner) are untouched — verified by grep.

**Concrete edits:**
- `charly.yml` — deletes `check-preempt-arbiter-pod`, `check-preempt-live-pod`, `check-preempt-vm-live` and their comment banners. A follow-up commit (`0143f57`) restores 8 lines of the `check-cross-vm-http` doc banner that the first hunk had removed by mistake.

## Change class

**candy/box-config** (box/distro project config: a bed removal).

**Closes / relates to:** relates to opencharly/plugin-preempt#8 (the receiving side, merged). relates to opencharly/charly#625 (the companion that removes the OTHER relocated beds from charly's own `charly.yml`; this PR is the `distro-fedora` leg of that same sweep). No ordering dependency on #625.

**Breaking change + rollback:** removes three bed names from this project. They are relocated, not deleted — consumers run them at `opencharly/plugin-preempt`. Rollback is a revert (the beds return here; the plugin copy would then be the duplicate).

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** `charly box validate` (config gate) + a live bed that exercises the retained shared fixture, since removing entities must not break the beds that remain.
- **Eval beds run — the EXACT bed + result:**
  - `check-cross-vm-http | vm | ok | 12 steps | ~300s` — deliberately chosen: it is the REMAINING bed that shares `web-vm`, so its green result proves the removal did not break the fixture's other consumer.
  - Ran on `charly version` = **2026.249.2125** (built from `opencharly/charly` origin/main `777d9a6c`), repo-override active against this tree; calver `.check/check-cross-vm-http/2026.266.0611/`.
- **Fresh rebuild:** the binary was built from a fresh `origin/main` worktree; `charly version` = 2026.249.2125; the bed ran at zero un-allowlisted warnings.
- **Did the CHANGED code path execute live?** The change is an entity removal; the live execution that matters is the REMAINING consumer of the shared `web-vm` (`check-cross-vm-http`), which ran green — proving the removal is scoped correctly. The removed beds' own behavior is proven in opencharly/plugin-preempt#8.
- **Concurrency (shared-state changes only):** N/A — a config entity removal; no shared state.

**Evidence — put each item in the block whose label names it.**

*1. Fresh-rebuild gate output* (`charly version` + the gate run, at zero warnings):
```
$ /tmp/opencode/charly-fixed/bin/charly version
2026.249.2125

$ charly box validate
charly box validate: OK — checked 441 candies, 38 boxes, 21 deploys, 7 distros, 7 builders; 0 warnings, 0 errors

$ charly check run check-cross-vm-http
charly check run check-cross-vm-http: [vm-build] PASS after 3.021s
charly check run check-cross-vm-http: [vm-create] PASS after 2.423s
charly check run check-cross-vm-http: PASS (steps=12)
```

*2. Each changed code path executing* (the lines proving the changed branch ran, not that it built):
```
# The three removed entities are the ONLY diff (net 207 deletions: 215 removed, 8 banner
# lines restored by 0143f57); no other entity changes:
$ git diff origin/main...HEAD -- charly.yml | grep -E "^-check-preempt|^[-+]check-cross|^[-+]web-vm|^[-+]fedora-vm"
-check-preempt-arbiter-pod:
-check-preempt-live-pod:
-check-preempt-vm-live:
# The shared fixture web-vm SURVIVES (its other consumer runs green):
$ grep -nE "^web-vm:|^check-cross-vm-http:" charly.yml
505:check-cross-vm-http:
595:web-vm:
charly check run check-cross-vm-http: PASS (steps=12)
```

*3. Docs-only evidence:* N/A — candy/box-config change class.

*4. Raw removal sweep (B14(d), R5) — all three removed identifiers, against this tree:*
```
$ cd distro-fedora && for id in check-preempt-arbiter-pod check-preempt-live-pod check-preempt-vm-live; do
    echo "--- $id ---"; git grep -n "$id" -- . ; echo "exit=$?"; done
--- check-preempt-arbiter-pod ---
CHANGELOG/2026.203.1029.md:19:`check-preempt-arbiter-pod`/`check-preempt-live-pod` over the same synthetic
CHANGELOG/2026.216.0232.md:17:`check-stepkind-emit-pod`, `check-enc-pod`, `check-tunnel-pod`, `check-preempt-arbiter-pod`), so
exit=0
--- check-preempt-live-pod ---
CHANGELOG/2026.203.1029.md:7:`check-preempt-live-pod` bed, which only proves the pod-venue branch
CHANGELOG/2026.203.1029.md:19:`check-preempt-arbiter-pod`/`check-preempt-live-pod` over the same synthetic
CHANGELOG/2026.250.0253.md:10:- **check-preempt-live-pod / check-preempt-vm-live** — see RCA below; ...
CHANGELOG/2026.250.0253.md:19:- check-preempt-live-pod: bring-up-members stopped the promoted root ...
CHANGELOG/2026.250.0253.md:37:charly check run check-preempt-live-pod: PASS (steps=21) ...
CHANGELOG/2026.250.0253.md:45:- check-preempt-live-pod: check-live PASS (8.5s) ...
exit=0
--- check-preempt-vm-live ---
CHANGELOG/2026.203.1029.md:1:# ... check-preempt-vm-live: R10 witness ...
CHANGELOG/2026.203.1029.md:3:New disposable bed `check-preempt-vm-live` ...
CHANGELOG/2026.205.0918.md:9:`check-preempt-vm-live`'s leading comment block still named ...
CHANGELOG/2026.229.1620.md:125:... `check-preempt-vm-live`
CHANGELOG/2026.250.0253.md:10:- **check-preempt-live-pod / check-preempt-vm-live** — see RCA below; ...
CHANGELOG/2026.250.0253.md:38:charly check run check-preempt-vm-live: PASS (steps=19) ...
CHANGELOG/2026.250.0253.md:46:- check-preempt-vm-live: check-live "33 steps: ..."
exit=0
# => EVERY hit is CHANGELOG history (R5-permitted). No live charly.yml/manifest reference remains.
```

*4. Emitted-artifact verification (R8):* N/A — no build/registry/merge defaults changed; `charly box validate` (0/0) is the config gate, and the retained fixture's live consumer (`check-cross-vm-http`) is proven in block 2.

## Attribution tier

**fully tested and validated.** The removal was proven scoped correctly by the remaining consumer of the shared `web-vm` fixture (`check-cross-vm-http`, 12 steps) executing live from a fresh rebuild at zero un-allowlisted warnings, plus a clean `charly box validate` (0/0). The removed beds' own behavior is proven in the merged receiving PR (opencharly/plugin-preempt#8).

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** loaded `check` (bed authoring/R10), `image` (box/distro config), `git-workflow`, `root-cause-analyzer`.
- **R1 RCA + zero warnings:** the removal's scope (keep `web-vm`, remove only the 3 preempt beds) was established by grepping every `web-vm` reference (found `check-cross-vm-http`) — not assumed; `charly box validate` is 0 warnings / 0 errors and the bed ran zero un-allowlisted warnings.
- **R2 no out-of-scope:** this IS the source-side half of the relocation, its own immediate cutover (the receiving side already merged as plugin-preempt#8); no deferral.
- **R3 no duplication:** the removal eliminates the duplicate bed definitions — the beds live in ONE place (the plugin repo) after this lands.
- **R4 no workaround:** no sleep/retry/magic-number; the entities are removed in full, not disabled or renamed.
- **R5 hard cutover + grep:** the three bed identifiers are removed completely. RAW sweep output pasted in block 2: every hit for all three identifiers is CHANGELOG history; ZERO live code/config reference remains. The retained `web-vm`/`check-cross-vm-http` are untouched (its other consumer proven live).
- **R6 git safety:** fresh worktree off `origin/main` (this session's own); `git status` inspected before staging; commit on `feat/` branch, PR-only, no force-push.
- **R7 runtime gate:** a live bed ran (the retained fixture's consumer) from a fresh rebuild — proving the removal did not break what remains.
- **R8 artifact invariants:** N/A — no generation/label change.
- **R9 binary == source + deps:** the binary was rebuilt from a fresh `origin/main` worktree; `charly version` = 2026.249.2125; no new runtime OS deps.
- **R10 disposable + coverage:** the retained consumer proved live on a `disposable: true` bed (`check-cross-vm-http`, 12 steps) at zero un-allowlisted warnings; `charly box validate` confirms the config is coherent.
- **RDD / ADE / SDD:** RDD — the "which entities does `web-vm` serve?" question was settled by grepping all references and proving the retained consumer live, not assumed. ADE — N/A (no candy plan changed; the fixture candies moved to the plugin repo intact). SDD — N/A (no schema/.cue edit).
- **Hard cutover:** ONE atomic commit (`597760d`); the 3 beds removed in full, no Phase-2.
- **Kernel/plugin boundary law:** N/A — a distro-repo config edit; the beds moved OUT to their plugin's owning repo, the boundary law's direction.
- **Disposable-only autonomy:** N/A — no autonomous destroy/rebuild outside the `disposable: true` bed.
- **Clean architecture + Go gates:** N/A for Go (no Go change); `charly box validate` is the config gate (0/0).
- **CHANGELOG:** N/A — this repo's `CHANGELOG/` is written from the PR body by the merge workflow. No file staged.

---

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*