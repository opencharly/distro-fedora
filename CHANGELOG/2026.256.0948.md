# 2026.256.0948 — docs: correct the tutorial-shell init description

## Summary

Correct the `tutorial-shell` box `description:` and plan comment, which claimed the init candy was "deliberately absent from the list" and auto-injected, while the box explicitly composes `layer-supervisord:v2026.240.0121`.

## RCA (full, unelided evidence)

The premise was proven with a real `charly box generate` on a fixture that composes `pod-sshd` without any supervisord candy in the scanned set:

```
$ cat /tmp/reftest2/charly.yml
ref-test2:
    candy:
        description: probe the corrected README refs resolve
        base: quay.io/fedora/fedora:43
        distro: ["fedora:43", fedora]
        candy:
            - '@github.com/opencharly/layer-ripgrep:v2026.235.1653'
            - '@github.com/opencharly/pod-sshd:v2026.239.1637'
            - '@github.com/opencharly/layer-charly:v2026.241.1407'
        plan:
            - check: probe
              command: "true"

$ charly box generate -C /tmp/reftest2 2>&1 | grep 'warning: box'
warning: box "ref-test2" resolves init "supervisord", which depends on the "supervisord" candy, but no candy of that name is in this project's scanned set — nothing was injected, so the image will declare init "supervisord" with no supervisord binary and fail at start. Reference the candy directly in the box's candy: list, or pull it in via a composed candy's require:.
```

The box under edit does exactly what the warning prescribes — it lists the init candy (candy list, outside the prose hunks):

```
$ sed -n '19,24p' box/tutorial-shell/charly.yml
        base: fedora
        candy:
            - '@github.com/opencharly/layer-supervisord:v2026.240.0121'
            - '@github.com/opencharly/layer-ripgrep:v2026.235.1653'
            - '@github.com/opencharly/pod-sshd:v2026.239.1637'
        plan:
```

So the explicit `layer-supervisord` is REQUIRED and the prose was stale (R1 documentation divergence). The docs site and the charly README derive claims from this box, so the source is fixed first (R4a).

## R5 — the replicated claim is swept by named sibling cutovers

Both consumers are open, same-cutover PRs that remove the same stale claim:

- **opencharly/docs#119** (`feat/retire-fleet`) — replaced the copied tutorial-shell quote (which claimed the init was absent/auto-injected) with a sentence + a link to the generated box reference.
- **opencharly/charly#604** (`feat/docs-truth`) — corrected the README's "Note what is *not* listed: an init system" paragraph to "the init candy is composed explicitly; without it charly warns and injects nothing".

No unfunded parking: each consumer has a named, open cutover.

## Rulebook compliance

- R1 — RCA above, with full output.
- R2 — no parking; both sibling cutsovers are open and named.
- R3 — description and plan comment now restate the same mechanism by design: the description explains the WHY for readers, the comment explains it where the check is authored. This is complementary, not the duplicated-explanation pattern; the plan `# comment` is the canonical explanation and the description is the published prose.
- R4 — no workaround; the warning text is quoted from the tool.
- R5 — the claim is swept via docs#119 + charly#604 (named above).
- R6 — worktree edit, PR-only.
- R7 — N/A — no behavior change (description/comment prose only).
- R8 — N/A — no emitted artifact changed.
- R9 — N/A — no binary/source runtime change.
- R10 — N/A — documentation-only change class (prose in a `description:` block + YAML comment); no runtime path changed, so no bed applies.

## Change classification

- Class: documentation-only (`box/tutorial-shell/charly.yml` `description:` block + inline comment).
- Gate: documentation reviewed.
- Confidence: **documentation reviewed**.

*Assisted-by: opencode deepseek-v4.1-flash (documentation reviewed)*