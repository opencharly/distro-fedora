# 2026.241.1006 — chore: migrate charly.yml schema to 2026.240.1943 (version bump only)

## Summary

spec #63 advanced `#SchemaVersion` from `2026.232.0520` to `2026.240.1943`. Every project repo must carry the new stamp before a consumer pinned to that spec can load it. This is a one-line version stamp produced by `charly migrate` — no structural edits.

```
-version: 2026.232.0520
+version: 2026.240.1943
```

This repo is also charly's `box/fedora` submodule and the fixture for `TestMaterializeSeam_RealFixtureDeterministic`, which fails on charly#446 today with exactly this schema mismatch.

## How tested

The previous revision of this body proved only that the old string was absent. A grep cannot show the config is *accepted* under the new schema, so the gate below is charly actually parsing and running this tree at the new spec.

**1 — `charly box validate` on this PR's tree**, charly built from source at `spec v0.2026241.931`:

```
$ charly box validate
charly box validate EXIT=0
```

**2 — a full `disposable: true` bed on this PR's tree**, `check-pod`, fresh rebuild, **no local overrides** (the plugin resolves from its published tag):

```
stage                    ok     errors  warnings  allowlisted
image-build              true   0       0         1
check-image              true   0       0         1
deploy-add               true   0       0         1
config                   true   0       0         1
start                    true   0       0         1
bring-up-members         true   0       0         1
feature-run              true   0       0         1
update                   true   0       0         5
check-live-rebuild       true   0       0         1
feature-run-rebuild      true   0       0         1
cleanup                  true   0       0         1
cleanup-members          true   0       0         15

total errors: 0 | total warnings: 0
charly check run check-pod: PASS (steps=13)
```

The `[update]` stage is charly's destroy-and-rebuild leg, so this covers a fresh rebuild rather than a warm reuse. The `allowlisted` entries are the dev/worktree-build notice, one per charly invocation.

**3 — the two-way control that makes the pass meaningful.** The identical bed on the identical tree, run *before* `plugin-deploy-pod v2026.241.0959` was published, failed at `[config]`:

```
charly check run check-pod: [config] FAIL after 3.513s: exit 1
charly check run check-pod: FAIL (steps=4)

deploy:pod config: InvokeProvider deploy:pod op=config-setup:
  resolving project-declared volumes: .../charly.yml:
  config schema 2026.240.1943 is newer than this charly supports (max 2026.232.0520).
```

So the pass above is not incidental: it is the direct consequence of the plugin fix landing, and it proves this stamp is now safe.

## Landing order — satisfied

`plugin-deploy-pod` resolves out-of-process at its own spec pin. Until today that pin's ceiling was `2026.232.0520`, below this stamp, so every pod deploy in this repo failed at `charly config`. That is now fixed and published:

- **opencharly/plugin-deploy-pod#2** — merged, tagged **`v2026.241.0959`**.

The bed in section 2 resolves `@github.com/opencharly/plugin-deploy-pod -> v2026.241.0959 (latest tag)` with no override, so the blocking dependency is satisfied in published form. The same constraint applies to the four sibling PRs (`distro-arch#30`, `distro-cachyos#32`, `distro-debian#20`, `distro-ubuntu#20`) and is recorded on each.

## Rulebook compliance

- **R1** — the bed failure this stamp triggered was root-caused to the plugin's own spec pin and fixed at the cause, not worked around. The first hypothesis (a stale local fixture) was ruled out by migrating it and re-running, which reproduced the failure unchanged.
- **R2** — no parking: the blocking defect was fixed and landed, not deferred.
- **R4** — no workaround; the stamp is produced by `charly migrate`, not hand-edited.
- **R6** — no force-push, no direct push to `main`, no hook bypass.
- **R7/R10** — the gate is a `disposable: true` bed on this PR's tree, pasted above, at zero errors and zero warnings.

## Change classification

- **Change class:** config (schema version stamp, 1 line)
- **Verification gate:** `charly box validate` EXIT 0, plus `charly check run check-pod` on a `disposable: true` target, fresh rebuild, `PASS (steps=13)`, 0 errors / 0 warnings, with a two-way control
- **Attribution tier:** `fully tested and validated` — the migrated tree was parsed, built, deployed, rebuilt and torn down live on a disposable target with no local overrides, at zero warnings, and the pass is isolated by a two-way control.

*Assisted-by: Claude Code Anthropic Opus 5 (fully tested and validated)*