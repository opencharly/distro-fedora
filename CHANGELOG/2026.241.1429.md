# 2026.241.1429 — fix: repoint the candy refs at the extracted standalone repos

## Summary of changes

The candy de-submodule cutover moved these candies out of `charly/candy/` into their own
kind-prefixed repos. This repo still pinned them by a path **inside charly**
(`@github.com/opencharly/charly/candy/<name>:<charly tag>`).

**10 refs across 7 files** now point at the standalone repo at its own current
tag.

| was `charly/candy/…` | now |
|---|---|
| `<name>` | `layer-<name>` |
| `charly` | `layer-charly` |
| `charly-mcp` | `pod-charly-mcp` |
| `check-stack-layer` | `layer-check-stack-layer` |
| `examplestep-consumer` | `layer-examplestep-consumer` |
| `stepkind-build-consumer` | `layer-stepkind-build-consumer` |

### Why this is not cosmetic

These refs are **not broken today** — they name *old* charly tags that still contain the
directories. That is precisely the failure this cutover exists to remove: **content frozen at a
stale pin, with every gate staying green.** charly's `main` no longer has those directories at all
(`opencharly/charly#451`), so the next pin bump would turn a silent staleness into a hard break.

## Change class

**box-config (ref pins)** — `@github` pin rewrites plus the header comment describing them. No box
logic, no build steps, no Go, no CI.

**Closes / relates to:** Phase D.6 of the cutover; consumes the extraction PRs in
`layer-charly-*` / `pod-charly-mcp` (all merged and tagged) and `opencharly/charly#451`.


**Breaking change + rollback:** none — the same candy content, fetched from its own repo instead of
from a frozen copy inside charly. Rollback is a plain revert.

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** `charly box validate` on the final tree. It **resolves every
  ref over the network**, so a bad repo name, a bad tag, or a name conflict fails it — this is not a
  textual assertion.
- **Eval beds run:** `N/A (ref pins)` — no `disposable: true` bed maps to a pin rewrite; the
  authority is the resolving validate.
- **Fresh rebuild:** validated with a charly binary built from current `main`.
- **Did the CHANGED code path execute live?** **Yes** — every repointed ref was fetched and resolved
  during validation.
- **Concurrency:** `N/A`.

*1. Fresh-rebuild gate output* (zero warnings):
```
$ charly box validate          # in this repo, final tree
exit=0
                               # zero non-noise output lines
```

*2. Each changed code path executing:*
```
(a) No ref into charly's candy/ survives:

$ grep -rn '@github.com/opencharly/charly/candy/' --include='*.yml' .
                               # no matches

(b) The validate that proves the new refs RESOLVE (not merely parse):

$ charly box validate          -> exit 0
    …which fetched each repointed repo at its pinned tag.
```

*3. Docs-only evidence:*
```
N/A — not docs-only: ref pins.
```

## Attribution tier

**fully tested and validated** — `charly box validate` passed on the final tree with a binary built
from current charly source, at zero warnings, and that command resolves every rewritten ref over the
network rather than just parsing it.

## Harness rulebook compliance

- **R0 skills:** `/charly-internals:git-workflow` loaded before the first git call.
- **R1 RCA + zero warnings:** each repointed ref was resolved live rather than assumed correct. Zero warnings.
- **R2 no out-of-scope:** nothing surfaced that is not fixed here.
- **R3 no duplication:** one pin per candy, pointing at the one repo that owns it.
- **R4 no workaround:** every new pin is a MERGED tag on the owning repo — no branch refs, no
  version-less refs, no re-vendoring.
- **R5 delete legacy completely:** the old path form is gone, and the header comment that described the old convention is corrected in the same commit.
- **R6 git safety:** branch cut from a fresh clone of `main`, `git diff --stat` reviewed before
  committing, `git show --stat` after. No force-push, no `--no-verify`, no push to `main`.
- **R7 runtime gate:** proven by a validate that performs network resolution, not by reading the diff.
- **R8 / R9:** `N/A` — no artifact emitted; validating binary built from current charly source.
- **R10 disposable + coverage:** the coverage that would fail without this change is the validate
  itself once charly's tags age out of carrying the deleted directories.
- **RDD / ADE / SDD:** RDD — that each new pin resolves was PROVEN by fetching it, not inferred from
  the repo existing. ADE — no candy content, plan step or box logic changes.
- **Hard cutover:** ONE atomic commit.
- **Kernel/plugin boundary law / Disposable-only autonomy:** `N/A`.
- **Clean architecture + Go gates:** `N/A — no Go source in this repo.`
- **CHANGELOG:** `N/A` — `tag-on-merge` writes it from this body at merge.

---

*Assisted-by: Claude Code claude-opus-5[1m] (fully tested and validated)*