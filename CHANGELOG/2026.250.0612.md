# 2026.250.0612 — chore: retire the fleet vocabulary in comments — charly fleet add → charly deploy add (CC-2 sweep)

## Summary

Cutover C task 2 (the fleet-vocabulary retirement) — the distro-fedora CC-2 sweep. The fleet vocabulary is RETIRED (operator ruling: usage frequency is never an argument to keep unclean code); this PR sweeps the one remaining vocabulary reference in this repo's charly.yml comment:

- `charly fleet add` → `charly deploy add` (the CLI surface rename, plugin-fleet command:deploy)

Comment-only change — no behavior, no schema, no pins.

## How tested

```
$ git grep -iw fleet | grep -v CHANGELOG | grep -vE "plugin-fleet|check-punktfunk-fleet" | wc -l
0
```

The claim-keyed sweep is ZERO outside historical CHANGELOGs (remaining matches are the plugin-fleet repo name, which keeps its name).

## Rulebook compliance

- **R0**: git-workflow skill loaded before the first tool call.
- **R1**: no anomalies — comment-only sweep, no failures.
- **R3**: one canonical name per concept — no dual representation survives.
- **R4**: no workarounds — the rename is complete, not aliased.
- **R5**: the old identifiers are DELETED, not aliased.
- **R6**: PR-only landing, no force-push, no direct main push.
- **R7**: the sweep gate proven above.
- **R10**: verified from the committed tree.

## Change classification

- **Change class**: docs/comment sweep (no pins, no scripts, no schema).
- **Verification gate**: documentation reviewed — comment-only change class.
- **Attribution tier**: documentation reviewed.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*