# 2026.205.0100 — Finish the HostArbiter R5 claim-keyed prose sweep (companion charly#184)

The prior commit on this branch (`f741154`, "docs(preempt): sweep stale
'HostArbiter reverse channel' prose after its retirement") swept four of the
five stale `HostArbiter` occurrences left after `ExecutorService.HostArbiter`'s
retirement (companion `opencharly/charly` PR #184, K1-unblock wave 1:
resource-arbiter gather/resources move off `HostArbiter` onto the generic
`Executor.HostBuild("resolved-project")` envelope). One occurrence remained —
`check-preempt-vm-live`'s leading comment block still named
`ExecutorService.HostArbiter RPC` in a contrastive aside explaining that the
VM-venue stop/start dispatch leg is unrelated to the (separately retired)
gather/resources leg.

Per R5 (claim-keyed sweep, `/charly-internals:strict-policy`), a comment naming
a deleted identifier is swept regardless of whether the naming is itself
historically accurate — the only permitted non-CHANGELOG residuals are
legitimate test-assertions-of-absence, which this was not (it's a prose
comment, not a test assertion). Reworded to describe the mechanism
(the resolved-project envelope leg) without naming the retired identifier.
Zero behavior change — prose only.

Evidence: `git grep -n HostArbiter -- ':!CHANGELOG'` in this repo now returns
empty.

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; documentation reviewed)
