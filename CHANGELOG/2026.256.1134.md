# 2026.256.1134 — docs: correct fedora box README

## Summary

Align the fedora box README with the corrected `CLAUDE.md` signpost and remove
the stale "(disabled)" labels.

- `README.md` no longer says the repo **vendors no candies** (false — the local
  `candy/charly-marketplace` closure manifest exists and is discovered via the
  `discover:` block). It now reads **vendors no shared candy layers**, matching the
  corrected `CLAUDE.md`.
- `charly-fedora` and `fedora-test` are not disabled: neither box stanza carries
  `enabled: false`, so the "(disabled)" labels are removed.
- Candy refs moved off `opencharly/charly/candy/*` into standalone repos; the
  README now cites the real
  `@github.com/opencharly/<layer-*|pod-*|plugin-*>[:subdir]:<tag>` shape.
- The schema-version hard-fail message is `"newer than this charly supports"`,
  not the quoted "update charly".

## How tested

Documentation-only change; no runtime gate. Every correction was verified against
the live tree and the claim-keyed sweeps were run over the whole repo.

Stale candy-ref shape and "single tag" claim — CHANGELOG history only in `*.md`;
the single `*.yml` hit is the `charly.yml` header comment that documents the
anti-pattern (`charly/candy/<name>` must NOT be used), not a live ref:

```
$ grep -rn 'charly/candy' --include='*.md' .
./CHANGELOG/2026.241.1429.md:5:The candy de-submodule cutover moved these candies out of `charly/candy/` into their own
./CHANGELOG/2026.241.1429.md:7:(`@github.com/opencharly/charly/candy/<name>:<charly tag>`).
./CHANGELOG/2026.241.1429.md:12:| was `charly/candy/…` | now |
./CHANGELOG/2026.241.1429.md:63:$ grep -rn '@github.com/opencharly/charly/candy/' --include='*.yml' .
./CHANGELOG/2026.231.1402.md:42:Every `@github.com/opencharly/charly/candy/...` pin in this repo moves from
./CHANGELOG/2026.227.1110.md:4:`@github.com/opencharly/charly/candy/*` pins in this repo onto ONE tag,
exit=0

$ grep -rn 'charly/candy' --include='*.yml' .
./charly.yml:11:#  repo, so a ref through `charly/candy/<name>` would pin a stale copy at whatever
exit=0

$ grep -rn 'single tag' .
exit=1
```

Stale "no candies" / "no `candy/` dir" claim — no remaining hit:

```
$ grep -rn 'vendors no candies\|no candy/ dir' --include='*.md' --include='*.yml' .
exit=1
```

Both show-case boxes carry no disable flag — the full stanzas:

```
$ sed -n '1,40p' box/charly-fedora/charly.yml
charly-fedora:
    candy:
        base: fedora
        # Builder maps do NOT cross the `charly` namespace boundary; route the
        # standard build formats to main's fedora-builder under the `charly`
        # namespace so any pixi/npm/cargo layer added here resolves.
        builder:
            pixi: fedora-builder
            npm: fedora-builder
            cargo: fedora-builder
        candy:
            - '@github.com/opencharly/layer-agent-forwarding:v2026.243.0408'
            - '@github.com/opencharly/layer-supervisord:v2026.240.0121'
            - '@github.com/opencharly/layer-charly:v2026.241.1407'
            - '@github.com/opencharly/layer-golang:v2026.239.1614'
            - '@github.com/opencharly/layer-gh:v2026.239.1624'
            - '@github.com/opencharly/pod-sshd:v2026.239.1637'
            - '@github.com/opencharly/layer-container-nesting:v2026.239.1647'
            - '@github.com/opencharly/layer-nvidia:v2026.236.116'

$ sed -n '1,40p' box/fedora-test/charly.yml
fedora-test:
    candy:
        base: fedora
        # Builder maps do NOT cross the `charly` namespace boundary; testapi carries
        # a pixi.toml, so route pixi (plus the standard npm/cargo) to main's
        # fedora-builder under the `charly` namespace.
        builder:
            pixi: fedora-builder
            npm: fedora-builder
            cargo: fedora-builder
        candy:
            - '@github.com/opencharly/layer-agent-forwarding:v2026.243.0408'
            - '@github.com/opencharly/pod-traefik:v2026.240.0203'
            - '@github.com/opencharly/pod-testapi:v2026.240.0203'

$ grep -rn 'enabled' box/charly-fedora/charly.yml box/fedora-test/charly.yml
exit=1
```

The schema hard-fail quote is the live source string:

```
$ grep -rn 'newer than this charly supports' /home/atrawog/Sync/Atrapub/oo/opencoder/opencharly/sdk/loaderkit/load_unified.go
83:			"%s: config schema %s is newer than this charly supports (max %s). Update charly (reinstall the latest opencharly package, or run 'task build:binary' from a fresh checkout and use ./bin/charly)",
```

## Rulebook compliance

- **R1** — every divergence was root-caused (candy de-submodule cutover; local
  closure manifest) before rewriting; no "pre-existing" classification.
- **R2** — the whole cutover is finished: `README.md` was the only surface still
  carrying the false "no candies" self-contradiction; the claim-keyed sweep above
  covers the `*.md` surfaces and the `charly.yml` header, with no live stale ref.
- **R3 — no duplication** — documentation-only; the corrected wording lives once in
  `README.md`, and the `CLAUDE.md` signpost points at the same rule without
  restating a divergent copy.
- **R4** — N/A — documentation-only; no sleeps, retries, suppressions, or
  fallback branches are introduced.
- **R4a — binary-only commands** — every command the README tells a reader to run
  (`charly box build`, `charly -C box/fedora box build`, `charly --repo
  opencharly/distro-fedora box build`) works with only the `charly` binary; no
  clone, `cd`, or `task` target is required.
- **R5** — the stale "no candies" claim and the false "(disabled)" labels are
  removed completely (not shimmed); the sweeps above are the self-test.
- **R6** — edits were made in the fresh `/tmp/opencode/distro-fedora-refs`
  worktree of the submodule (never in the umbrella checkout); `git status` was
  clean before editing; landing is PR-only, no force-push, no direct push to
  `main`.
- **R7** — N/A — documentation-only; no runtime behavior changed, so no test can
  fail without the change.
- **R8** — N/A — documentation-only; no labels, plans, configs, schemas, or
  generated files are emitted or changed.
- **R9** — N/A — documentation-only; no binary is built or invoked here.
- **R10** — N/A — documentation-only; no image is generated or deployed and no
  bed is invented (R10's docs clause).

## Change classification

documentation-only → `documentation reviewed` (the whole diff is markdown:
`README.md` plus the `CLAUDE.md` signpost; no pins or scripts changed).

*Assisted-by: opencode deepseek-v4.1-flash (documentation reviewed)*