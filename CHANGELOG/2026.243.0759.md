# 2026.243.0759 — chore: complete the pin advance charly#506 started

## Summary
    layer-docs-site             v2026.239.1618 -> v2026.243.0408
    pod-dsh                     v2026.239.1643 -> v2026.243.0410
    pod-nested-podman-socket    v2026.239.1601 -> v2026.243.0411

charly#506 advanced 11 stale candy pins in charly's own charly.yml. This repo's
candy/charly-marketplace pins three of the same candies and was not advanced with it, so charly
composes each at TWO versions and warns. The three new versions here are exactly the ones charly
pins.

## Two copies, and why fixing one was not enough

candy/charly-marketplace is a per-repo COPY of the same pin list. distro-cachyos and
distro-fedora each carry one, as does the standalone marketplace repo, so one candy's version
must be advanced in every copy independently.

Measured from charly's root, which is where the composition and therefore the split occurs:

    before any fix                          6 warnings
    after distro-cachyos#56 (copy 1 of 2)   3 warnings

Missing the second copy left the split half open while looking fixed. That is the same shape as
the plugin-wl chain earlier in this session: a repin landing one link deep leaves every other
path stranded.

This commit is the second copy. The count reaches 0 only once charly also advances its fedora
import pin onto the tag this produces - charly's candy SCAN resolves an import from the PINNED
cache, not from the local box/fedora checkout, so a working-tree fix alone does not move it.
That measurement is pasted in the charly PR that advances the pin, not claimed here.

## How tested

    charly box validate
    exit 0

## Rulebook compliance

- **R1 — RCA every anomaly.** Six warnings surfaced when charly's `main` merged into a working
  branch. Traced through charly's own fetch cache to the exact manifests, and when fixing the
  first copy moved the count only 6 → 3, the remaining path was traced rather than the partial
  result being reported as done.
- **R2 — Finish the whole cutover.** Not parked as "pre-existing" despite originating in
  charly#506; it surfaced during an active cutover, so it is fixed. charly#506 is the named
  batch this completes, and its remaining link (charly's fedora import pin) is named explicitly
  with the reason a working-tree fix alone cannot move the count.
- **R3 — No duplication.** The pins advance to exactly the versions charly already pins. The
  DEEPER duplication — `candy/charly-marketplace` existing as three independent copies of one
  ~180-entry pin list — is named as the underlying defect; collapsing it spans the distro repos
  and the marketplace generator and cannot be proved by this change's gate.
- **R4 — No workarounds.** No allowlist entry; the split is closed at its cause.
- **R6 — Git safety.** Branched from a fresh `origin/main`; no force-push, no hook bypass, no
  direct push to main.
- **R7 — Prove behavior, not compilation.** `charly box validate` pasted here. The composed
  count is measured from charly's root and the numbers given are the ones actually observed;
  the final `0` is deliberately NOT claimed here, because it depends on a pin advance that is
  not in this diff.

## Change classification

| | |
|---|---|
| **Change class** | pin advance (three `@github` refs in one candy manifest) |
| **Verification gate** | `charly box validate` here; the composed 6 → 3 → 0 progression is measured from charly's root across this PR and the charly pin advance |
| **Attribution tier** | `analysed on a live system` — validate passed and the composed warning count was measured live before and after each copy; no bed in this repo exercises these three candies |

*Assisted-by: Claude Code claude-opus-4-6-20260401 (analysed on a live system)*