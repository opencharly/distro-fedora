# 2026.243.0857 — fix(box): advance agent-forwarding pin in sway-browser-vnc (dire-242.1147 shell drop-in fix)

## Summary

Fix the failing sway-browser-vnc checkbed's `layer-direnv:2/:4` shell drop-in checks by advancing the composed `layer-agent-forwarding` pin, and align every other ref in the distro to it (zero-warning reconcile).

**RCA (proven via charly's own surfaces):** the sway image composed `layer-agent-forwarding:v2026.239.1607`, whose internal pin `layer-direnv:v2026.237.504`'s plan asserts `/etc/profile.d/charly-layer-direnv-bash.sh` while the `shell:` emitter names drop-ins after the candy entity (`charly-direnv-*`, candy name `direnv`) — the checks could never pass on that chain. The corrected `layer-direnv:v2026.242.1147` (checks aligned to `charly-direnv-*`) is pinned by `layer-agent-forwarding:v2026.243.0408`.

**Changes:**
- `box/sway-browser-vnc/charly.yml`: `layer-agent-forwarding:v2026.239.1607` → `v2026.243.0408`
- 20 more refs across the distro aligned to `v2026.243.0408` (multi-version resolution warning → zero-warning: the bed build was warning "resolved to multiple versions" while 1607 lingered in other boxes/candy refs).

## How tested — pasted output (fresh-rebuild bed run, zero warnings)

`charly check run check-sway-browser-vnc-pod` against a FRESH rebuild with the changed pin + fully aligned refs:

```
charly check run check-sway-browser-vnc-pod: [image-build] PASS after 1m25.128s [allowlisted=1 cache=325/349]
charly check run check-sway-browser-vnc-pod: [check-image] ... 188 steps: 148 passed, 0 failed, 40 skipped
  PASS  check the bash shell-init drop-in EVALs the direnv hook ... [layer-direnv:2]  ok
  PASS  check the fish shell-init drop-in ... [layer-direnv:4]  ok
charly check run check-sway-browser-vnc-pod: [start] PASS after 2.526s
charly check run check-sway-browser-vnc-pod: [check-live] PASS ... [check-live-rebuild] PASS after 2m12.349s
charly check run check-sway-browser-vnc-pod: [feature-run-rebuild] PASS after 1m58.41s
charly check run check-sway-browser-vnc-pod: [cleanup] PASS ... [cleanup-members] PASS
charly check run check-sway-browser-vnc-pod: PASS (steps=13)
```

Multi-version resolution warnings in the run log: **0** (the alignment cleared them).

The two previously-failing steps (`layer-direnv:2` bash drop-in, `layer-direnv:4` fish drop-in) are now **PASS**; the run concludes **PASS (steps=13)**.

## Rulebook compliance

- R1: root cause established through charly's new `check plan` verb (resolved paths vs image contents) + tag inspection, not assumed.
- R4: no workaround — advancing pins to already-landed upstream corrections.
- R10: fresh-rebuild bed run pasted above, zero warnings, failing steps green.
- R6: no destructive actions; single repo, pin-only.

## Change classification

- Change class: pin bump (config) — runtime-affecting checkbed fix.
- Verification gate: R10 fresh-rebuild bed run, pasted (steps=13 PASS, 0 warnings).
- Attribution tier: fully tested and validated (fresh rebuild of the changed tree, bed run pasted, zero warnings).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*