# Schema compaction — compact node grammar, SSH port auto-allocation, Calamares residue removed

`charly migrate` (compact-node-form, schema 2026.186.2323) reshaped every
charly.yml to the compact grammar (inline collections, ordered `plan:` lists,
`<word>: <input>` verb sugar, live-verb fields folded into their input maps,
`env:` maps). Full `charly box validate` green against the new loader.

- Removed the Calamares `check-target` box (`box/check-target/`) — it referenced
  the deleted `candy/check-target-layer`, so every box/fedora bed failed to load
  until it was removed (the superproject's Calamares deletion left this residue;
  the bed gate caught it).
- check-fedora-vm / check-cross-vm-http and the other VM beds now AUTO-ALLOCATE
  their SSH host port (`ssh: {port_auto: true}`) for collision-free parallel runs.
