# 2026.229.1246 — the check-ollama-pod R10 bed (ollama candy + command:ollama plugin)

Adds the disposable `check-ollama-pod` bed: a pod deploy of the `ollama` image
that runs the candy's inherited baked checks PLUS three host-side
`charly ollama` steps against the live server over the auto-allocated host
port (`${HOST_PORT:11434}`) — `help` dispatch, `version`, and `list` — proving
the compiled-in `command:ollama` plugin (opencharly/charly's
`candy/plugin-ollama`) end-to-end.

Verified: `charly -C box/fedora check run check-ollama-pod` passed at zero
warnings on the full 13-step sequence (66 live checks, 0 FAIL) with
CHARLY_REPO_OVERRIDE pinned to the in-development superproject.
