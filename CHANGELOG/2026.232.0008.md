# 2026.232.0008 — live-proof the auto-merge body reconciliation end-to-end

PR #25 ("fix(ci): reconcile the PR body when the CHANGELOG is finalized at
merge time") landed the 9th-defect fix on `distro-fedora` main. The new behavior
it ships — the finalize step appends an idempotent `## Auto-merge finalization`
note to the PR body naming the actual finalized file, so the re-run validator's
review of the finalized head stays body↔diff-consistent (A1) — cannot be proven
by the stub-gh harness alone; it must be exercised by a live PR through the
corrected workflow.

This PR is that live proof. Its body deliberately pins this placeholder filename
(`CHANGELOG/2026.232.0008.md`); the corrected workflow must now finalize it to
the merge-time CalVer, append the body note naming the finalized file, re-run
the validator to green, merge, and mint the release tag. The merge + tag of this
PR is the recorded proof. Only when it lands AND its tag is minted do I
propagate the byte-identical workflow to the other opencharly repos (the
propagation script is gated on this proof).

## Change class

**docs-only** — a single new `CHANGELOG/2026.232.0008.md` markdown file, renamed
to the merge-time CalVer by the auto-merge finalizer at landing. The workflow
change itself landed in PR #25; this PR carries none. The `documentation
reviewed` tier applies.

*Assisted-by: Claude Code deepseek-v4-flash:cloud (documentation reviewed)*
