# 2026.241.0740 — fix: advance the pod-chrome-headless pin - charly box validate now exits 0

## Summary

`box/chrome-headless` pinned `pod-chrome-headless:v2026.240.0121`, which still reaches
`pod-chrome-cdp:v2026.235.2110`. That tag names its dependencies with the pre-cutover **bare**
form, so they resolve to nothing and abort two boxes:

```
candy "github.com/opencharly/pod-chrome-cdp" depends: unknown candy "plugin-cdp"
candy "github.com/opencharly/pod-chrome-cdp" candy: unknown candy "chrome-devtools-mcp"
global candy order: resolving candies for image "sway-browser-vnc": unknown candy "chrome-devtools-mcp"
box "chrome-headless":   candy resolution error: unknown candy "chrome-devtools-mcp"
box "sway-browser-vnc":  candy resolution error: unknown candy "chrome-devtools-mcp"
```

`pod-chrome-headless#2` advanced its own `pod-chrome-cdp` pin to `v2026.240.1559`, where those
deps are proper `@github` refs, and `tag-on-merge` produced `v2026.241.0642`. This advances one
line to that tag.

## How tested

Two-way, same binary, on the final committed tree with a clean worktree:

```
$ charly box validate          # pod-chrome-headless:v2026.240.0121
exit=1, 5 validation errors

$ charly box validate          # pod-chrome-headless:v2026.241.0642
exit=0, 0 validation errors
```

**This repo now validates completely clean.**

## What this closes

These are exactly the five errors `#39`'s body recorded as remaining and routed to the
post-cutover plugin-candy pin reconciliation batch, with the cause traced there to
`pod-chrome-cdp@v2026.235.2110`'s bare names. That routing is now discharged rather than left
open — the batch member is closed, not re-deferred.

It also unblocks `sway-browser-vnc`: `charly box build` validates the whole project before
building any box, so that box could not be built while these errors stood.

## Rulebook compliance

- **R1** — the errors were traced to a specific pinned tag's bare-name deps, and fixed at the pin
  rather than worked around.
- **Rule 5 / Pin discipline** — `v2026.241.0642` is a merged tag on `pod-chrome-headless`'s
  default branch, produced by `tag-on-merge` for its PR #2.
- **R7 / R10** — measured on the final committed tree, both states pasted.
- **Rule 1** — this repo's own box file only.

## Change classification

- **Class:** one candy pin advanced to a merged tag.
- **Verification gate:** `charly box validate` two-way in this repo — exit 1 with 5 errors to
  exit 0 with none.
- **Attribution tier:** `analysed on a live system` — resolution exercised live with retained
  output and the repo's validation gate now passing. No bed was run on this branch, so I am not
  claiming `fully tested and validated`.

*Assisted-by: Claude Code Anthropic Opus 5 (analysed on a live system)*