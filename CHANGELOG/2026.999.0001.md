# 2026.999.0001 (placeholder — pr-validator finalizes at merge)

The box/fedora leg of the K-wave seam-death terminus: one bed-correctness fix.

- **check-cross-pod-cdp**: `cdp-web-fixture-rendered` gains an `eventually: 90s` budget (was 30s).
  The 30s budget was smaller than one wedged CDP call cycle (measured 37.8s under load), so the
  step's retry semantics were vacuous — a single hung evaluate consumed the whole budget and the
  "eventually" never retried. 90s covers one wedged cycle plus a real retry with margin; the
  measurement is cited in the step comment. Found by the terminus roster's OOM-storm RCA
  (chrome renderers killed mid-evaluate); the companion product fix (the CDP call timeout deriving
  from the step budget instead of a hardcoded 30s) landed in the superproject leg.
