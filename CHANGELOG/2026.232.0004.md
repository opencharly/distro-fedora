# 2026.232.0004 — live-proof the corrected auto-merge workflow

PR #23 — "fix(ci): read the release-tag merge commit from the flat
`merge_commit_sha` field" — landed the corrected auto-merge workflow on `main`
(the `merge_commit_sha` flat-field read so every merge mints its release tag).
The merge itself was performed by the OLD workflow (it was still on `main` when
PR #23 opened), which — per the defect it fixed — merged without minting the
release tag. This change backfilled `v2026.232.1544` add-only on the merged
HEAD and opens this PR as the **live proof** that the corrected workflow
processes a full landing cycle end-to-end:

trigger on the validator's completion → wait for EVERY status check to be green
→ finalize the CHANGELOG placeholder to the merge-time CalVer → wait for /
re-run the validator on the finalized head → merge via GitHub auto-merge (no
direct-squash fallback) → **mint the release tag `v<YYYY.DDD.HHMM>` on the
merged HEAD**.

This is the required canary-first gate before the byte-identical workflow is
propagated to the other 12 opencharly repos (`apply.sh` is gated on this PR's
live proof).

*Assisted-by: Claude Code deepseek-v4-flash:cloud (documentation reviewed)*
