# 2026.232.1304 — adopt charly schema 2026.232.0520

The spec repo's strict-cleanup cutover (`github.com/opencharly/spec`, PR #42)
bumped `#SchemaVersion` from `2026.225.1508` to `2026.232.0520` — the
`remove-candy-localpkg` (2026.225.1508) and `install-template-to-phases`
(2026.232.0520) migration entries. This repo's root `charly.yml` is re-stamped
via `charly migrate` to the head schema version.

**Pure version adoption.** The config carries no `install_template` and no
`localpkg` surface, so the migration is a stamp-only change (1 line): the schema
gate now accepts the project under the merged spec. The nested `box/*/charly.yml`
configs carry per-kind candy CalVers, not schema stamps, and are untouched.

The consumer landing for the same cutover in `opencharly/charly` loads this repo
as a fixture (`TestMaterializeSeam_RealFixtureDeterministic`), which is what makes
this adoption load-bearing rather than cosmetic.
