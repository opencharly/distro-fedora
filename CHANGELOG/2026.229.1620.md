# 2026.229.1620 — declare source.distro on the cloud_image VMs

CalVer minted at merge by the pr-validator and shared with this release's git tag
`v2026.229.1620`. Verified free of filename and tag collision before stamping.

`source.distro` selects three things about a guest that nothing else in the spec implies: its
package NAME (`openssh` vs `openssh-server`), its package MANAGER (`pacman -Sy --needed` vs the
`packages:` cloud-config key) and its sshd UNIT name (`sshd` vs `ssh`).

It used to be INFERRED from `base_user`, and only for `arch` and `alpine` — every other distro
fell through to Arch/Fedora conventions. So a Debian-family image that omitted it was rendered
`openssh` + `systemctl enable --now sshd`, a unit Debian and Ubuntu do not have, and with
cloud-init's deliberate `mask ssh.socket` that left a socket-activated sshd masked and never
started: the guest booted fully and served nothing.

That inference is **being removed** in opencharly/spec#35 + opencharly/sdk#140, both of which are
still OPEN at the time of writing. Once they land, `spec.DistroSSHUnits[distro]` and
`spec.DistroInits[distro]` become bare lookups into tables generated from
`schema/distro_vocab.cue` with **no default**, and an absent distro will render an EMPTY sshd unit
name rather than silently meaning "Arch/Fedora conventions". Presence will be enforced at author
time by the vm kind's own OpValidate capability, which currently lives on an unmerged superproject
branch. Nothing of that is landed yet, so this repo's cloud_image sources
must state `fedora` explicitly.

## What is true TODAY, and why this is safe to land first

`spec@origin/main` types the field `distro?: string` — optional **and open** — and carries no
`schema/distro_vocab.cue` at all. So on today's main this change is **purely additive**: it cannot
trip closed-schema rejection and cannot break any current consumer. The closed `#DistroID`
vocabulary, the generated trait tables and the presence check all arrive later, with spec#35 /
sdk#140 / the superproject change.

That also settles the landing order: this repo can, and should, land FIRST. Once the sdk lookups
lose their default, a consumer repo that still omits `distro:` renders an empty unit name — so the
field wants to be in place before that, not after.

## Why this repo has to land before its consumers

Consumers pin this repo by tag. Their suites fail against the currently-pinned copy for exactly
this reason, so the tag minted on merge is what unblocks them.

## Gate

Pasted here rather than referenced. An earlier revision of this entry said "full output is
pasted in the PR", which made a shipped artifact depend on a PR body. A CHANGELOG must carry its
own evidence.

**Provenance: this output came from a POST-CUTOVER binary** — one built from a worktree carrying
the unmerged spec/superproject changes — not from any released `charly` and not from `main`. That
matters because the errors below exhibit the presence enforcement and the closed vocabulary, and
this entry states elsewhere that neither is landed yet (`spec@origin/main:vm.cue` still has
`distro?: string`, and main carries no `schema/distro_vocab.cue`). Both readings are correct; only
the provenance was missing, and without it the paste reads as current shipped behaviour. The
output is real, reproducible from that tree, and describes the behaviour this cutover lands — not
the behaviour a user has today.

```
$ charly -C box/fedora box validate   # CONTROL — PR head unmodified
exit=0

$ charly -C box/fedora box validate   # MUTATION — distro removed from source #1 (must FAIL)
charly: error: command "validate": validation error: /home/atrawog/Atrapub/coder/claude/charly/.claude/worktrees/phase3-nfpm-cutover/box/fedora/charly.yml:doc0: node "fedora-vm": kind "vm" validation failed: source.distro: must be declared for a cloud_image source (one of [almalinux alpine arch archarm cachyos centos debian endeavouros fedora manjaro rhel rocky ubuntu]) — it selects the guest's package name, package manager and sshd unit name, and nothing else in the spec implies it
exit=1

$ charly -C box/fedora box validate   # MUTATION — distro removed from source #2 (must FAIL)
charly: error: command "validate": validation error: /home/atrawog/Atrapub/coder/claude/charly/.claude/worktrees/phase3-nfpm-cutover/box/fedora/charly.yml:doc0: node "web-vm": kind "vm" validation failed: source.distro: must be declared for a cloud_image source (one of [almalinux alpine arch archarm cachyos centos debian endeavouros fedora manjaro rhel rocky ubuntu]) — it selects the guest's package name, package manager and sshd unit name, and nothing else in the spec implies it
exit=1

$ charly -C box/fedora box validate   # MUTATION — distro: redhat, not in the vocabulary (must FAIL)
               explicit error (_|_ literal) in source:
                   7143:17
exit=1
```

Both added lines are individually load-bearing (removing either fails by its own node name) and an id outside the generated
vocabulary fails on CUE closedness.

**Tier is `fully tested and validated`.** The R10 bed ran, on this repo, and passed:

```
charly check run check-fedora-vm: [vm-build] PASS after 797ms 
charly check run check-fedora-vm: [vm-create] PASS after 1.442s 
charly check run check-fedora-vm: [deploy-add] PASS after 2m9.392s 
charly check run check-fedora-vm: [bring-up-members] PASS after 0s
charly check run check-fedora-vm: [check-live] PASS after 5.916s 
charly check run check-fedora-vm: [update] PASS after 3m33.186s 
charly check run check-fedora-vm: [check-live-rebuild] PASS after 5.899s 
charly check run check-fedora-vm: [cleanup] PASS after 2.512s 
charly check run check-fedora-vm: [cleanup-members] PASS after 0s
charly check run check-fedora-vm: PASS (steps=9)
```

**An earlier revision of this entry claimed the bed "currently cannot" run, and that was wrong.**
I reasoned that every project-loading command fails on the poisoned `distro-arch` pin and concluded
the gate was unreachable — circular by construction. It is not: `box/fedora/charly.yml:37` is
`import: []`, so this repo resolves no `@github` namespace and the poisoned pin cannot reach it. The
breakage is real but scoped to the SUPERPROJECT root. Invoked as `charly -C box/fedora check run
<bed>` — which is how this submodule's beds are meant to be driven — the gate was reachable the
whole time. A validator ran it and told me so. **I derived an obstacle instead of attempting the
command**, which is the same error this entry criticises elsewhere.

The bed exercises the changed entity directly (`check-fedora-vm` composes `fedora-vm`), and it is
the render that matters: the guest booted, cloud-init ran with `distro: fedora`, sshd came up on the
unit that field selects, and the fresh-rebuild leg repeated it.

**What closedness does NOT prove:** a wrong-but-valid id passes. `distro: debian` on a Fedora
image validates exit 0 — verified by a fresh validator. So the value was checked against the image
URL by hand: this repo's source is `download.fedoraproject.org/.../Fedora-Cloud-Base-Generic-*.qcow2`,
corroborated by `base_user: fedora`.

**Bounding the coverage claim, because a gap reported as a pass is worse than a gap.** There is no
executed control proving today's consumers are unaffected. A validator tried the released binary
rather than inferring, and it refuses this repo outright:

```
config schema 2026.225.1508 is newer than this charly supports (max 2026.223.1018)
```

`origin/main` is refused **identically**, so the rejection is the schema floor, not `distro:`. No
released `charly` can read this repo at all, which means the compatibility claim rests on reading
the schema and sources, not on running anything. Worth knowing on its own: this repo's schema floor
already outpaces the shipped package.

Coverage is also bounded on the bed side. `check-fedora-vm` and `check-cross-vm-http` cover the
render of both changed entities — which is what `distro:` selects — but `check-preempt-vm-live`
also composes `web-vm` and was NOT run; it adds preemption mechanics `distro:` cannot reach.
