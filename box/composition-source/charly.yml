composition-source:
    candy:
        base: fedora
        candy:
            - '@github.com/opencharly/layer-supervisord:v2026.240.0121'
            - '@github.com/opencharly/layer-charly:v2026.241.1407'
        plan:
            - check: command=test -x /usr/bin/charly
              id: composition-source-charly
              exit_status: 0
              command: test -x /usr/bin/charly
            - check: command=test -d /etc
              id: composition-source-marker
              exit_status: 0
              command: test -d /etc
