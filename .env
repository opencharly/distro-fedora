# Test-bed credential store for the encrypted-volume (gocryptfs) check beds.
# The passphrase is stored into the charly credential store (charly/enc/<box>)
# so the ExecStartPre `charly config mount` can resolve it under systemd.
GOCRYPTFS_PASSWORD=charly-bed
